# messages.parse + output_config.effort + thinking={"type":"adaptive"} требуют
# свежий SDK: на anthropic<0.9x эти вызовы падают с TypeError.
fastapi>=0.115
uvicorn[standard]>=0.32
pydantic>=2.9
anthropic>=0.121,<1.0

# Турнирная площадка: SQLite при разработке, Postgres на Railway.
sqlalchemy>=2.0,<3.0
psycopg[binary]>=3.2
argon2-cffi>=23.1
httpx>=0.27
